import { Montserrat, Sora } from "next/font/google";

export const monserrat = Montserrat({ subsets: ["latin"], weight: ["600"] });

export const sora = Sora({ subsets: ["latin"], weight: ["600", "700"] });
